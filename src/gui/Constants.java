package gui;

public interface Constants {
    final static double GAME_WIDTH_PROP = 0.78;       //Width of the game portion (prop of total)
    final static double GAME_HEIGHT_PROP = 1.0;      //Height of the game portion (prop of total)

    //Dimensions of the error pane (in pixels)
    final static int ERROR_WIDTH = 500;
    final static int ERROR_HEIGHT = 150;
    final static double INFO_SIZE = 0.5;    //How much of the screen should the info make up?

    final static String ROOT = System.getProperty("user.dir") + "/images/";
}
