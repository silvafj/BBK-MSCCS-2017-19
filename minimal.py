# This is a minimal tkinter program
from tkinter import *
import random

app = Tk() # Create the top-level window
app.title("GUI Example") # OPTIONALLY set the title
app.geometry('400x400')  # OPTIONALLY set the size

app.mainloop() # Start the main loop
